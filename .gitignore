roms/
*.gb
*.gbc
*.sav
*.state
testdata/fixtures/
failure/
artifacts/
.env
# Local build leftovers and the homelab Swarm overlay (registry, hosts,
# Traefik). Public docs for the local farm are deploy/README.md.
/pokepilot
/pokewall
deploy/farm.swarm.yml
deploy/SWARM.md
deploy/swarm.sh
repro-*

# Vue/Vite frontend workspace and generated assets. The placeholder keeps the
# embed directory valid for Go-only development; production/CI replace it with
# independently built operator and spectator bundles before compiling pokeui.
/web/node_modules/
/web/dist/
/web/src/shared/redWorldManifest.generated.ts
/cmd/pokeui/ui/vue/*
!/cmd/pokeui/ui/vue/placeholder.txt
!/cmd/pokeui/ui/vue/_underscore_embed_probe.txt
/tailwindpro
# Build-time mirror of pinned pret/pokered render assets.
/web/public/gen1/

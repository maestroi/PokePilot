# Nothing ROM- or save-state-shaped may enter the Docker context or image.
roms/
*.gb
*.sav
*.state
skill/failure/
.git
# Root build binaries, if any were left behind by a local `go build`.
/pokepilot
/pokewall
# Frontend dependencies/output are rebuilt deterministically inside the image.
web/node_modules/
web/dist/
cmd/pokeui/ui/vue/operator/
cmd/pokeui/ui/vue/spectator/

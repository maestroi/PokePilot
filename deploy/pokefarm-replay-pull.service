[Unit]
Description=Reconcile the PokePilot replay sidecar on the iGPU worker
After=docker.service
Requires=docker.service

[Service]
Type=oneshot
ExecStart=/usr/local/sbin/pokefarm-replay-pull
Nice=10
